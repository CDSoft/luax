Third-party modules written in C (possibly with some Lua)
=========================================================

These modules can not be bundled with the pure Lua implementation of `luax.lua`
(unless it is reimplemented in Lua in `luax-libs`).
